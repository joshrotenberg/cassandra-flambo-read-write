(ns cassandra-flambo-read-write.core-test
  (:require [clojure.test :refer :all]
            [cassandra-flambo-read-write.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
